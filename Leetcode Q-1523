class Solution {
public:
    int countOdds(int low, int high) {
        int count = 0;
        for(int i = low; i <= high; i++){ //iterating i.
            if(i % 2 != 0){ // checking if i is odd or not.
                count++;
            }
        }
        return count;
    }
};
